---
import { t } from '../copy';
---

<section class="band" id="needs">
  <div class="section intro">
    <div class="intro-head">
      <span class="aj-chip">B</span>
      <h2 data-copy="needs.h2">{t('needs.h2')}</h2>
      <p class="lead mt-5 max-w-[760px]" data-copy="needs.lead">{t('needs.lead')}</p>
    </div>
  </div>
</section>
